dbURI = 'mongodb+srv://<username>:<password>@cluster0.0em0x.mongodb.net/myFirstDatabase?retryWrites=true&w=majority'

# when finalize replace tje uri with
# mongodb+srv://<username>:<password>@main-uat.6rr6v.mongodb.net/myFirstDatabase?retryWrites=true&w=majority
# in readme add
# Replace <password> with the password for the developer user. Replace myFirstDatabase with the name of the database that connections will use by default.
